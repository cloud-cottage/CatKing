{
  "builds": [
    {
      "src": "Gemfile",
      "use": "@vercel/ruby",
      "config": {
        "bundleInstallCommand": "bundle install",
        "buildCommand": "bundle exec jekyll build"
      }
    }
  ],
  "outputDirectory": "_site"
}
